"""Challenge 5 — Username Validation Engine

Create a username validator.

Rules:

-   Minimum length: 5
-   Must contain at least one number
-   Must not contain spaces
-   Must not start with a number

The program should:

1.  Ask the user for 5 usernames.
2.  Validate each username.
3.  Store valid usernames separately from invalid usernames.
4.  Display reasons why a username failed validation.

----------

## Requirements

-   Use:
    -   Functions
    -   If/Elif/Else
    -   Logical operators
    -   Lists

----------

## Sample Output

```text
john12 -> VALID
12mark -> INVALID (cannot start with number)
mary -> INVALID (too short)

```

----------

## Assessment Criteria

-   Proper validation logic
-   Good function structure
-   Accurate failure messages

----------"""

# how to run
1. clone the repository

```bash
git clone <repository-url>

2. Navigate to the project directory:

```bash
cd project-folder
``

3. Run the script:

```bash
python challenge5.py