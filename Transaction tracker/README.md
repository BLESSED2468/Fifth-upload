"""
# Challenge 7 — Customer Transaction Tracker

You are building a transaction tracker for a small business.

Each transaction contains:

```python
(transaction_id, customer_name, amount, status)

```

The system should:

1.  Store at least 10 transactions.
2.  Separate successful and failed transactions.
3.  Calculate:
    -   Total successful transaction amount
    -   Average transaction amount
4.  Display the top 3 highest successful transactions.
5.  Allow searching by customer name.

----------

## Requirements

-   Use:
    -   Tuples
    -   Lists
    -   Sorting
    -   Functions
    -   Looping
    -   Conditional logic

----------

## Constraints

-   You must sort manually OR use `sorted()` with a lambda function.
-   Search must be case-insensitive.

----------"""

# how to run
1. clone the repository

```bash
git clone <repository-url>

2. Navigate to the project directory:

```bash
cd project-folder
``

3. Run the script:

```bash
python challenge8.py
